/**
 * These properties can safely be ignored on changes.
 * They will otherwise cause unwanted replacement of VMs
 */
export const IgnoreableVmProperties = ["audioDevice", "cdrom", "disks", "initialization", "vga"]
